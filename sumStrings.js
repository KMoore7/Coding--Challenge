function sumStr(a,b) {  //declare function anmed sumStr with parameters (a,b)
    return Number(a) + Number (b) +'' //return the sum of a+b as numbers and then turn into a string by adding ''
    
  }
